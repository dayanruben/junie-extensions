{
  "name": "java-engineer",
  "description": "Java 17–21 policy and pitfalls. Enforces Optional / sealed / switch-exhaustiveness / virtual-threads rules and catches the subtle language traps (Stream.toList mutability, orElse vs orElseGet, carrier pinning, pattern-matching null) that LLMs get wrong by default."
}
